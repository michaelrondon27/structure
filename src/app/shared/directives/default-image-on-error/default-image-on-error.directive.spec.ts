import { DefaultImageOnErrorDirective } from './default-image-on-error.directive';

describe('DefaultImageOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultImageOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
